code versionctrl.txt       -- opens the file in vscode

git init    --   is used to intialise


git status  --   provide files which are not in the stagging area

git add versionctrl.txt  -- used to add the versionctrl.txt file into the stagging area

git commit -m "message"   --  used to commit


git log  --  used to check the commit and their timings

git diff versionctrl.txt      --  check the differences made after after commit 


git checkout versionctrl.txt     --    we will get back to the previous version code.

